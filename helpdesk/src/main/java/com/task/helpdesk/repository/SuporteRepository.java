package com.task.helpdesk.repository;

import com.task.helpdesk.model.Support;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SuporteRepository extends JpaRepository<Support, Long> {
}
